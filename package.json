{
  "name": "silent-plan-arg",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-assets.mjs && node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "serve": "node scripts/serve.mjs"
  }
}

